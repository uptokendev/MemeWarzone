name: Import review isolated build kit
on:
  push:
    branches: [diagnose/import-review-build-kit-20260909]
    paths: [.github/workflows/import-review-build-kit.yml]
permissions:
  contents: read
jobs:
  prepare:
    runs-on: ubuntu-latest
    timeout-minutes: 8
    steps:
      - uses: actions/checkout@v4
        with:
          ref: c0ce2e9ba4eac69fe3d8b5f6395a3a86f47e7b7e
          path: mwz
          persist-credentials: false
      - uses: actions/checkout@v4
        with:
          repository: pump-fun/pump-public-docs
          path: pump-docs
          persist-credentials: false
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: npm ci --prefix mwz/frontend --no-audit --no-fund
      - name: Record sources and documentation
        run: |
          git -C mwz rev-parse HEAD > mwz/SOURCE_SHA
          git -C pump-docs rev-parse HEAD > pump-docs/SOURCE_SHA
          mkdir -p references
          curl -fsSL --max-time 20 https://supabase.com/changelog.md > references/supabase-changelog.md
          cd mwz
          CLI_VERSION=$(npm view supabase version)
          printf '%s\n' "$CLI_VERSION" > ../references/supabase-cli-version
          npx --yes supabase@$CLI_VERSION --version
          npx --yes supabase@$CLI_VERSION migration new --help
          npx --yes supabase@$CLI_VERSION migration new project_import_review_evidence
      - name: Pack only development source and installed dependencies
        run: |
          tar --exclude='.git' --exclude='.env' --exclude='.env.*' -czf /tmp/import-review-kit.tar.gz mwz/frontend mwz/supabase mwz/SOURCE_SHA pump-docs references
      - uses: actions/upload-artifact@v4
        with:
          name: import-review-build-kit
          path: /tmp/import-review-kit.tar.gz
          retention-days: 2
