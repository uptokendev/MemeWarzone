import fs from "node:fs";

const file = "scripts/test-bnb97-native-pending-graduation.ts";
let source = fs.readFileSync(file, "utf8");

const staleBlockedBuy = `  const blockedBuy = await expectCustomError(campaign, "GraduationPending", () =>\n    campaign.connect(buyer).buyExactBnbAuthorized(1n, pendingAuth.routeProfileId, pendingAuth.deadline, pendingAuth.signature, { value: ethers.parseEther("0.001") }),\n  );`;
const freshBlockedBuy = `  const blockedBuyValue = ethers.parseEther("0.001");\n  const blockedBuyAuth = await buildTradeAuthorization(\n    signerMod,\n    campaign,\n    buyer,\n    routeAuthority,\n    TRADE_AUTH_BUY_EXACT_NATIVE,\n    blockedBuyValue,\n    1n,\n  );\n  const blockedBuy = await expectCustomError(campaign, "GraduationPending", () =>\n    campaign.connect(buyer).buyExactBnbAuthorized(\n      1n,\n      blockedBuyAuth.routeProfileId,\n      blockedBuyAuth.deadline,\n      blockedBuyAuth.signature,\n      { value: blockedBuyValue },\n    ),\n  );`;

const staleBlockedBuyOccurrences = source.split(staleBlockedBuy).length - 1;
if (staleBlockedBuyOccurrences !== 1) {
  throw new Error(`expected exactly one stale blocked-buy authorization fixture, found ${staleBlockedBuyOccurrences}`);
}
source = source.replace(staleBlockedBuy, freshBlockedBuy);

const rawCrossingMath = `  const restoredTarget = await campaign.graduationNativeTarget();\n  const healthyCrossing = restoredTarget * 2n;\n  await fund(deployer, buyer, healthyCrossing + ethers.parseEther("0.02"));\n  const [healthyQuoted] = await campaign.quoteBuyExactBnb(healthyCrossing);\n  const healthyMinOut = (healthyQuoted * 99n) / 100n;\n  const pendingAuth = await buildTradeAuthorization(signerMod, campaign, buyer, routeAuthority, TRADE_AUTH_BUY_EXACT_NATIVE, healthyCrossing, healthyMinOut);\n  const pendingTx = await campaign.connect(buyer).buyExactBnbAuthorized(\n    healthyMinOut,\n    pendingAuth.routeProfileId,\n    pendingAuth.deadline,\n    pendingAuth.signature,\n    { value: healthyCrossing },\n  );\n  const pendingReceipt = await pendingTx.wait();\n  if (parseEvent(campaign, pendingReceipt, "CampaignFinalized")) throw new Error("crossing BUY emitted CampaignFinalized");\n  if (!(await campaign.graduationPending()) || (await campaign.launched())) {\n    throw new Error("crossing BUY did not freeze pending-first");\n  }\n  const frozen = await campaign.getGraduationState();\n  const frozenTarget = await campaign.pendingGraduationNativeTarget();\n  const frozenRaised = await campaign.netRaisedWei();\n  if (frozen.graduationBalance !== frozenRaised) throw new Error("frozen graduationBalance drifted from netRaisedWei");\n  if (frozenTarget === 0n) throw new Error("pendingGraduationNativeTarget was not frozen");\n  if ((await topazFactory.getPool(info.token, await wbnb.getAddress(), false)) !== ethers.ZeroAddress) {\n    throw new Error("Topaz pool existed before completion");\n  }`;

const principalAwareCrossing = `  const restoredTarget = await campaign.graduationNativeTarget();\n  const raisedBeforeCrossing = await campaign.netRaisedWei();\n  if (raisedBeforeCrossing >= restoredTarget) {\n    throw new Error(\`below-threshold regression invalid: netRaisedWei=\${raisedBeforeCrossing} target=\${restoredTarget}\`);\n  }\n\n  const soldBeforeCrossing = await campaign.sold();\n  const curveSupply = await campaign.curveSupply();\n  const remainingCurveSupply = curveSupply - soldBeforeCrossing;\n  if (remainingCurveSupply <= 0n) throw new Error("no remaining curve supply available for deterministic crossing");\n\n  const maxTotalInput = await campaign.quoteBuyExactTokens(remainingCurveSupply);\n  const [maxQuotedTokens, maxQuotedTotalCost, maxQuotedFee] = await campaign.quoteBuyExactBnb(maxTotalInput);\n  const maxCostNoFee = maxQuotedTotalCost - maxQuotedFee;\n  if (maxQuotedTokens === 0n || raisedBeforeCrossing + maxCostNoFee < restoredTarget) {\n    throw new Error(\n      \`curve cannot reach graduation target: raised=\${raisedBeforeCrossing} target=\${restoredTarget} remaining=\${remainingCurveSupply} maxPrincipal=\${maxCostNoFee}\`,\n    );\n  }\n\n  let lowInput = 1n;\n  let highInput = maxTotalInput;\n  while (lowInput < highInput) {\n    const midInput = (lowInput + highInput) / 2n;\n    const [midTokens, midTotalCost, midFee] = await campaign.quoteBuyExactBnb(midInput);\n    const midCostNoFee = midTotalCost - midFee;\n    if (midTokens > 0n && raisedBeforeCrossing + midCostNoFee >= restoredTarget) highInput = midInput;\n    else lowInput = midInput + 1n;\n  }\n\n  const healthyCrossing = lowInput;\n  const [healthyQuoted, healthyTotalCost, healthyFeeWei] = await campaign.quoteBuyExactBnb(healthyCrossing);\n  const authoritativeCostNoFee = healthyTotalCost - healthyFeeWei;\n  if (healthyQuoted === 0n) throw new Error("deterministic crossing quote returned zero tokens");\n  if (raisedBeforeCrossing + authoritativeCostNoFee < restoredTarget) {\n    throw new Error("deterministic crossing quote does not cross authoritative no-fee target");\n  }\n  if (healthyQuoted > remainingCurveSupply) throw new Error("deterministic crossing exceeds remaining curve supply");\n\n  await fund(deployer, buyer, healthyCrossing + ethers.parseEther("0.02"));\n  const healthyMinOut = (healthyQuoted * 99n) / 100n;\n  const pendingAuth = await buildTradeAuthorization(signerMod, campaign, buyer, routeAuthority, TRADE_AUTH_BUY_EXACT_NATIVE, healthyCrossing, healthyMinOut);\n  const pendingTx = await campaign.connect(buyer).buyExactBnbAuthorized(\n    healthyMinOut,\n    pendingAuth.routeProfileId,\n    pendingAuth.deadline,\n    pendingAuth.signature,\n    { value: healthyCrossing },\n  );\n  const pendingReceipt = await pendingTx.wait();\n  const raisedAfterCrossing = await campaign.netRaisedWei();\n  const pendingAfterCrossing = await campaign.graduationPending();\n  const launchedAfterCrossing = await campaign.launched();\n  console.log("[bnb97-pending-crossing] authoritative crossing", {\n    netRaisedWeiBefore: raisedBeforeCrossing.toString(),\n    restoredTarget: restoredTarget.toString(),\n    remainingCurveSupply: remainingCurveSupply.toString(),\n    healthyCrossingInput: healthyCrossing.toString(),\n    healthyQuoted: healthyQuoted.toString(),\n    quotedTotalCostWei: healthyTotalCost.toString(),\n    quotedFeeWei: healthyFeeWei.toString(),\n    authoritativeCostNoFee: authoritativeCostNoFee.toString(),\n    netRaisedWeiAfter: raisedAfterCrossing.toString(),\n    graduationPending: pendingAfterCrossing,\n    launched: launchedAfterCrossing,\n  });\n  if (raisedAfterCrossing < restoredTarget) throw new Error("crossing BUY did not actually cross netRaisedWei target");\n  if (parseEvent(campaign, pendingReceipt, "CampaignFinalized")) throw new Error("crossing BUY emitted CampaignFinalized");\n  if (!pendingAfterCrossing || launchedAfterCrossing) {\n    throw new Error("crossing BUY did not freeze pending-first");\n  }\n  const frozen = await campaign.getGraduationState();\n  const frozenTarget = await campaign.pendingGraduationNativeTarget();\n  const frozenRaised = await campaign.netRaisedWei();\n  if (frozen.graduationBalance !== frozenRaised) throw new Error("frozen graduationBalance drifted from netRaisedWei");\n  if (frozenTarget === 0n) throw new Error("pendingGraduationNativeTarget was not frozen");\n  if ((await topazFactory.getPool(info.token, await wbnb.getAddress(), false)) !== ethers.ZeroAddress) {\n    throw new Error("Topaz pool existed before completion");\n  }`;

const rawCrossingOccurrences = source.split(rawCrossingMath).length - 1;
if (rawCrossingOccurrences !== 1) {
  throw new Error(`expected exactly one raw msg.value crossing fixture, found ${rawCrossingOccurrences}`);
}
source = source.replace(rawCrossingMath, principalAwareCrossing);

fs.writeFileSync(file, source);
console.log("bnb97_pending_harness_fresh_blocked_buy_auth=ok");
console.log("bnb97_pending_harness_principal_aware_crossing=ok");