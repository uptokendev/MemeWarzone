node_modules
cache
artifacts
typechain-types
types/ethers-contracts
coverage
coverage.json
dist

# Environment files - local only
.env
.env.*
**/.env
**/.env.*

# Sanitized templates are allowed
!**/.env*.example

# Private keys, certificates, credentials and local secret files
*.pem
*.key
*.p12
*.pfx
*.keystore
*.mnemonic
secrets.*
credentials.*
**/secrets.*
**/credentials.*

# Local deployment configuration
deployments/*.env
deployments/*.frontend.env
config/*.local
config/*.secret

# Generated deployment and report outputs
deployments/*.json
deployments/*.activation.json
deployments/*.retired.json
deployments/robinhood/testnet.staged.json
!deployments/robinhood/testnet.accepted.json
!deployments/bnb/*.json
output/*.json
output/testnet-cycle-results*.json
output/security-access-matrix.md
reports/topaz-graduation-testnet-*.json
reports/topaz-graduation-testnet-*.md
reports/bnb-lifecycle-certification-*.json
reports/robinhood-testnet-acceptance-5c.verify.json
reports/robinhood-testnet-acceptance-5c.continuity.json

# Local Netlify folder
.netlify

# Local archive accidentally created at repo root
frontend.zip

# ---------------------------------------------------------------------------
# Local AI / coding-agent tooling (never push to the remote repo)
# ---------------------------------------------------------------------------
.openclaw/
.grok/
.agents/
/agents/
/agents.md
AGENTS.md
!frontend/api/AGENTS.md

# Local agent workflow scripts (do not ignore GitHub CI under .github/workflows/)
/workflows/

# Local Grok / agent scratch (screenshots, copies, one-off scripts)
.grok-tmp/

# Nested git worktrees checked out inside this repo
.wt-*/

# Partial port / copy directories that are not source of truth
.port-to-dev/

# External checkout symlink — real Solana programs live in programs/ and solana/
/contracts/solana

# Unused one-off patch files
*.patch

# Accidental shell typo output (e.g. `git tatus --short`)
/tatus --short

# Downloaded pinned third-party program binaries (SHA256 is recorded in config/)
third_party/meteora/*.so
third_party/meteora/accounts/

# Rust / Anchor build output (never commit)
/target/
.anchor/
test-ledger/

# Incremental TS build info
*.tsbuildinfo

# Confidential build planning / co-architect communication (local only, never GitHub)
docs/build_plans/
