const resolvers = new Map();
export function registerProjectImportResolver(chainId,resolver){const id=Number(chainId);if(!Number.isSafeInteger(id)||id<=0)throw new Error("Invalid project import resolver chain id");if(typeof resolver!=="function")throw new Error("Project import resolver must be a function");resolvers.set(id,resolver);}
export function unregisterProjectImportResolver(chainId){resolvers.delete(Number(chainId));}
export function hasProjectImportResolver(chainId){return resolvers.has(Number(chainId));}
export async function resolveProjectToken(input){const chainId=Number(input?.chainId);const resolver=resolvers.get(chainId);if(!resolver){const error=new Error(`Project import ownership resolver is not configured for chain ${chainId}`);error.code="PROJECT_IMPORT_RESOLVER_UNAVAILABLE";throw error;}const result=await resolver(input);return{chainId:Number(result?.chainId??chainId),tokenAddress:String(result?.tokenAddress||input?.tokenAddress||"").trim(),name:result?.name==null?null:String(result.name),symbol:result?.symbol==null?null:String(result.symbol),decimals:result?.decimals==null?null:Number(result.decimals),totalSupply:result?.totalSupply??null,automaticOwnershipAvailable:Boolean(result?.automaticOwnershipAvailable),currentAuthority:result?.currentAuthority==null?null:String(result.currentAuthority),signedWalletMatchesAuthority:Boolean(result?.signedWalletMatchesAuthority)};}
