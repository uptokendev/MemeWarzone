# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
test-results/
playwright-report/
blob-report/
playwright/.cache/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# =============================================================================
# Agent Setup - NEVER COMMIT (local only)
# =============================================================================
# All agent-related configuration, skills, personas, and documentation feeds
# must remain private and never be pushed to the remote repository.

# Root agent rules document
AGENTS.md

# Entire .grok directory (contains custom skills, personas, architect feed, etc.)
.grok/
